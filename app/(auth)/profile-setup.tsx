import { ScreenPlaceholder } from "../../components/ScreenPlaceholder";

export default function ProfileSetupScreen() {
  return (
    <ScreenPlaceholder
      title="Profile Setup"
      eyebrow="Auth"
      description="Screen shell for rider name, username, city, language, and profile photo."
    />
  );
}
