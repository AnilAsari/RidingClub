import { ScreenPlaceholder } from "../../components/ScreenPlaceholder";

export default function GarageSetupScreen() {
  return (
    <ScreenPlaceholder
      title="Garage Setup"
      eyebrow="Auth"
      description="Screen shell for adding the rider's first bike to the garage."
    />
  );
}
