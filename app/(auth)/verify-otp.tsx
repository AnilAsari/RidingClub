import { ScreenPlaceholder } from "../../components/ScreenPlaceholder";

export default function VerifyOtpScreen() {
  return (
    <ScreenPlaceholder
      title="Verify OTP"
      eyebrow="Auth"
      description="Screen shell for OTP verification and session creation."
    />
  );
}
