import { ScreenPlaceholder } from "@shared/components/ScreenPlaceholder";

export default function OnboardingScreen() {
  return (
    <ScreenPlaceholder
      title="Onboarding"
      eyebrow="Auth"
      description="Screen shell for the Rovera value intro before phone login."
    />
  );
}
