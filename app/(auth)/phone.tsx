import { ScreenPlaceholder } from "@shared/components/ScreenPlaceholder";

export default function PhoneLoginScreen() {
  return (
    <ScreenPlaceholder
      title="Phone Login"
      eyebrow="Auth"
      description="Screen shell for entering the rider phone number and requesting OTP."
    />
  );
}
