import { ScreenPlaceholder } from "@shared/components/ScreenPlaceholder";

export default function ClubDetailScreen() {
  return (
    <ScreenPlaceholder
      title="Club Detail"
      eyebrow="Clubs"
      description="Screen shell for club profile, upcoming rides, members, posts, and join status."
    />
  );
}
