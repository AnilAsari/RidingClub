import { ScreenPlaceholder } from "@shared/components/ScreenPlaceholder";

export default function RoutePlannerScreen() {
  return (
    <ScreenPlaceholder
      title="Route Planner"
      eyebrow="Routes"
      description="Screen shell for drawing or searching biker-friendly routes with waypoints."
    />
  );
}
