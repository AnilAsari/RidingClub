import { ScreenPlaceholder } from "@shared/components/ScreenPlaceholder";

export default function OfflinePacksScreen() {
  return (
    <ScreenPlaceholder
      title="Offline Map Packs"
      eyebrow="Routes"
      description="Screen shell for downloading and managing state or region map packs."
    />
  );
}
