import { ScreenPlaceholder } from "@shared/components/ScreenPlaceholder";

export default function RouteDetailScreen() {
  return (
    <ScreenPlaceholder
      title="Route Detail"
      eyebrow="Routes"
      description="Screen shell for saved route details, stops, hazards, distance, and terrain."
    />
  );
}
