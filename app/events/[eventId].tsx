import { ScreenPlaceholder } from "@shared/components/ScreenPlaceholder";

export default function EventDetailScreen() {
  return (
    <ScreenPlaceholder
      title="Ride Event Detail"
      eyebrow="Group Rides"
      description="Screen shell for route preview, RSVP, attendees, countdown, and ride status."
    />
  );
}
