import { ScreenPlaceholder } from "@shared/components/ScreenPlaceholder";

export default function MyEventsScreen() {
  return (
    <ScreenPlaceholder
      title="My Events"
      eyebrow="Group Rides"
      description="Screen shell for upcoming rides the rider created or RSVP'd to."
    />
  );
}
