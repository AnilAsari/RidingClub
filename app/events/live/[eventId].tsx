import { ScreenPlaceholder } from "@shared/components/ScreenPlaceholder";

export default function LiveGroupRideScreen() {
  return (
    <ScreenPlaceholder
      title="Live Group Ride"
      eyebrow="Group Rides"
      description="Screen shell for live convoy map, member locations, and ride controls."
    />
  );
}
