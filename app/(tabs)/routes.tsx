import { ScreenPlaceholder } from "../../components/ScreenPlaceholder";

export default function RoutesScreen() {
  return (
    <ScreenPlaceholder
      title="Routes"
      description="Screen shell for route planning, saved routes, offline map packs, and rider stops."
    />
  );
}
