import { ScreenPlaceholder } from "@shared/components/ScreenPlaceholder";

export default function HomeScreen() {
  return (
    <ScreenPlaceholder
      title="Home Feed"
      description="Entry point for nearby rides, club activity, route posts, and rider discovery."
    />
  );
}
