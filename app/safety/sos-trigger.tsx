import { ScreenPlaceholder } from "@shared/components/ScreenPlaceholder";

export default function SosTriggerScreen() {
  return (
    <ScreenPlaceholder
      title="SOS Trigger"
      eyebrow="Safety"
      description="Screen shell for emergency countdown, location sharing, and cancel action."
    />
  );
}
