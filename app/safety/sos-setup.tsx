import { ScreenPlaceholder } from "@shared/components/ScreenPlaceholder";

export default function SosSetupScreen() {
  return (
    <ScreenPlaceholder
      title="SOS Setup"
      eyebrow="Safety"
      description="Screen shell for emergency contacts, SOS preferences, and test flow."
    />
  );
}
