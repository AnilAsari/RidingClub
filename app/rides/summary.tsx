import { ScreenPlaceholder } from "@shared/components/ScreenPlaceholder";

export default function RideSummaryScreen() {
  return (
    <ScreenPlaceholder
      title="Ride Summary"
      eyebrow="Ride Logging"
      description="Screen shell for post-ride stats, GPS track preview, badges earned, and sharing."
    />
  );
}
