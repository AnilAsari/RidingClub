import { ScreenPlaceholder } from "@shared/components/ScreenPlaceholder";

export default function RideDetailScreen() {
  return (
    <ScreenPlaceholder
      title="Ride Detail"
      eyebrow="Ride Logging"
      description="Screen shell for one completed ride, route map, stats, hazards, and notes."
    />
  );
}
