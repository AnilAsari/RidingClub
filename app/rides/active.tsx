import { ScreenPlaceholder } from "@shared/components/ScreenPlaceholder";

export default function ActiveRideScreen() {
  return (
    <ScreenPlaceholder
      title="Active Ride"
      eyebrow="Ride Logging"
      description="Screen shell for current solo ride tracking, speed, distance, timer, and SOS access."
    />
  );
}
