import { ScreenPlaceholder } from "@shared/components/ScreenPlaceholder";

export function AlertsScreen() {
  return (
    <ScreenPlaceholder
      title="Alerts"
      eyebrow="Notifications"
      description="Screen shell for ride reminders, RSVP updates, badge moments, club activity, and useful notification history."
    />
  );
}
